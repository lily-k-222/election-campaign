# VoiceConnect Campaign - PROJECT MODIFICATION LOG

이 문서는 프로젝트의 모든 수정 사항을 **시점, 문제, 수정방법, 향후 계획**의 형식으로 기록하는 공식 일지입니다.

---

## 📝 수정 일지 (Modification Log)

### [2026-03-19] 전용 통화 로그(call_logs) 시스템 도입
- **문제**: 기기 간 동기화 이슈나 실수로 인한 데이터 덮어쓰기 시 복구할 방법이 없음. 3월 18일 데이터 유실 사고 발생.
- **수정방법**:
    - 모든 통화 및 수정 내역을 독립적으로 저장하는 `call_logs` 테이블 신설.
    - 프론트엔드(`CampaignContext.jsx`)에서 저장 시마다 로그를 자동 생성하도록 로직 추가.
    - 관리자가 전체 로그를 조회할 수 있는 API(`fetchCallLogs`) 연동.
- **향후 계획**: 관리자 대시보드에 로그 조회 UI를 추가하여 실시간 모니터링 강화.

### [2026-03-18] 자원봉사자 연락처 할당 가시성 및 실시간 동기화 개선
- **문제**: 
    1. 특정 자원봉사자(장선영님)가 로그인 시 자신에게 할당된 연락처가 보이지 않음 (이메일 불일치로 인한 ID 마이그레이션 실패).
    2. 관리자가 연락처를 할당해도 자원봉사자 화면에 즉시 나타나지 않고 새로고침이 필요함.
- **수정방법**:
    - `fix_assignment_ids.cjs` 스크립트를 실행하여 예전 이메일 ID(`tjdwns05280404`)에 묶여있던 연락처 300여 건을 현재 UUID로 일괄 이전.
    - `CampaignContext.jsx`의 실시간 리스너를 수정하여 `UPDATE` 이벤트 발생 시 목록에 없는 데이터라면 즉시 추가하도록 로직 개선.
- **향후 계획**: 다른 관리자/자원봉사자 계정에서도 유사한 ID 불일치 사례가 있는지 전수 조사 및 실시간 알림 기능 강화 검토.

### [2026-03-18] Supabase 로그온 장애 및 RLS 권한 수정
- **문제**: 신규 사용자가 구글 로그인 시 프로필 생성이 안 되거나, 기존 사용자가 ID 마이그레이션(pending -> UUID)을 완료하지 못해 무한 루프에 빠짐.
- **수정방법**:
    - `users` 테이블에 `INSERT` 정책 추가 (본인 이메일 확인).
    - `users` 테이블의 `UPDATE` 정책을 수정하여 일반 사용자도 자신의 ID를 업데이트할 수 있도록 허용.
    - `AI_INSTRUCTIONS.md`를 생성하여 해당 RLS 규칙을 명문화.
- **향후 계획**: RLS 정책 변경 시 인증 흐름에 영향이 없는지 자동화된 테스트 스크립트 구축.

### [2026.03 후반] 관리자 기능 고도화 및 데이터 필터링 정상화
- **문제**: 담당자별 필터링이 작동하지 않고, 엑셀 내보내기 시 데이터 누락 발생. 에러 리포트 접근 권한이 개발자에게만 한정됨.
- **수정방법**:
    - `AdminDashboard.jsx`의 필터링 로직 및 엑셀 다운로드(`xlsx`) 함수 수정.
    - `error_reports` 테이블의 RLS 정책을 수정하여 `ADMIN` 역할도 접근 가능하도록 변경.
    - 지지 정도 구분 값을 사용자 요청에 맞게 변경 (강하게 지지, 보통 등).
- **향후 계획**: 대용량 데이터(1만 건 이상) 처리 시의 성능 최적화 진행.

### [2026.03 중반] Supabase 인증 및 데이터 마이그레이션
- **문제**: Firebase Auth에서 Supabase Auth로 이전하며 기존 사용자 데이터와의 연결 끊김 발생.
- **수정방법**:
    - `AuthContext.jsx`에 이메일 기반 자동 ID 동기화(Migration) 로직 추가.
    - 초기 RLS 정책 설정 및 무한 재귀 오류 해결.
- **향후 계획**: Firebase 데이터의 완전한 파기 및 Supabase 보안 강화.

### [2026.02] 프로젝트 초기화 및 배포 환경 구축
- **문제**: 인증 시스템 부재 및 로컬 개발 환경과 배포 환경(Vercel) 간의 불일치. 로그인 오류('Access Denied').
- **수정방법**:
    - Firebase 초기 연동 및 Vercel 자동 배포 파이프라인 구축.
    - Prisma DB 연결 안정화 및 Git 충돌 해결.
- **향후 계획**: 프로젝트 규모 확장에 따른 DB 아키텍처 재설계.

---

## 🛠️ 시스템 현황 요약
- **인증**: Supabase Auth (Google OAuth)
- **DB**: Supabase PostgreSQL (RLS 활성화)
- **주요 테이블**: `users`, `contacts`, `error_reports`, `announcements`, `settings`
